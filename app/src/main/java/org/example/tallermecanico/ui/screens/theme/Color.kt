package org.example.tallermecanico.ui.theme

import androidx.compose.ui.graphics.Color

// Colores base de la app
val AzulPrimario = Color(0xFF1565C0)
val AzulOscuro = Color(0xFF0D47A1)
val Celeste = Color(0xFF42A5F5)
val CelesteClaro = Color(0xFF90CAF9)

// Escala de grises
val GrisOscuro = Color(0xFF404040)
val GrisMedio = Color(0xFF757575)
val GrisTexto = Color(0xFF616161)
val GrisClaro = Color(0xFFF5F5F5)
val GrisClarisimo = Color(0xFFFAFAFA)

// Colores base del sistema
val Negro = Color(0xFF000000)
val Blanco = Color(0xFFFFFFFF)

// Colores de estado
val Verde = Color(0xFF4CAF50)   // Completada
val Naranja = Color(0xFFFF9800) // Pendiente
val Amarillo = Color(0xFFFFC107) // En proceso
val Rojo = Color(0xFFF44336)    // Errores u otros estados
